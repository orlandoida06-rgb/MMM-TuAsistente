import type { DuckDuckGoImage } from "./api.js";
export interface ExpectedResponse {
    results: DuckDuckGoImage[];
    next: string;
}
/**
 * Validate response shape
 */
export declare function isResponseValid(data: any): data is ExpectedResponse;
/**
 * Do a regular query to get a temporary session token used in image searches
 */
export declare function getToken(query: string): Promise<string>;
export declare function sleep(ms: number): Promise<unknown>;
